# agent-memory-auditor

## Purpose
Audit markdown memory files for duplicates, stale entries, and contradiction hints.

## Run
```bash
python3 src/audit.py --dir memory --out report.md --json report.json
```
